# figma-cli - 진행상황

마지막 업데이트: 2026-02-11

---

## 전체 진행 현황

| Phase | 내용 | 상태 | 비고 |
|-------|------|------|------|
| 0 | 구현 가능성 검증 (MCP 서버 통신 테스트) | **완료** | 8개 도구 전수 검증 |
| 1 | 프로젝트 초기화 (package.json, tsconfig, 빌드) | **완료** | |
| 2 | MCP 클라이언트 코어 (FigmaMCPClient, 세션, 8개 도구) | **완료** | 27개 테스트 통과 |
| 3 | CLI 프레임워크 (commander, 명령어 등록) | **완료** | program.ts + registerCommands |
| 4 | extract 명령어 (get_design_context) | **완료** | 핸들러 + 4개 테스트 |
| 5 | inspect 명령어 (get_metadata) | **완료** | 핸들러 + 3개 테스트 |
| 6 | shot 명령어 (get_screenshot) | **완료** | 핸들러 + 3개 테스트 |
| 7 | tokens 명령어 (get_variable_defs) | **완료** | 핸들러 + 3개 테스트 |
| 8 | connect 명령어 (code_connect_map) | **완료** | list/add 서브커맨드 + 4개 테스트 |
| 9 | design-rules / figjam 명령어 | **완료** | 각 핸들러 + 5개 테스트 |
| 10 | 에이전트 최적화 (--json, 에러 핸들링) | **완료** | formatting.ts + 전역 --json + handleError |
| 11 | Claude Code 스킬 제작 | **완료** | SKILL.md + references/cli-commands.md |

---

## 테스트 현황

| 테스트 파일 | 항목 수 | 상태 |
|-------------|---------|------|
| `tests/cli.test.ts` | 11 | 통과 |
| `tests/client/session.test.ts` | 8 | 통과 |
| `tests/client/transports.test.ts` | 3 | 통과 |
| `tests/client/FigmaMCPClient.test.ts` | 14 | 통과 |
| `tests/commands/extract.test.ts` | 5 | 통과 |
| `tests/commands/inspect.test.ts` | 3 | 통과 |
| `tests/commands/shot.test.ts` | 3 | 통과 |
| `tests/commands/tokens.test.ts` | 3 | 통과 |
| `tests/commands/connect.test.ts` | 4 | 통과 |
| `tests/commands/design-rules.test.ts` | 2 | 통과 |
| `tests/commands/figjam.test.ts` | 3 | 통과 |
| `tests/utils/formatting.test.ts` | 3 | 통과 |

**총 63개 테스트 통과**

---

## 다음 세션에서 할 일

1. npm link로 글로벌 설치 후 경로 의존성 제거
2. 스킬 반복 개선 (Step 6: Iterate) — 실제 사용 피드백 반영

---

## 핵심 설계 결정 요약 (다음 세션 참고용)

- SDK가 SSE 파싱, 세션 헤더 관리, initialize 시퀀스를 자동 처리 → 직접 구현 불필요
- `transportFactory` 옵션으로 테스트 시 mock Transport 주입
- Accept 헤더에 `application/json, text/event-stream` 필수 (누락 시 406)
- 세션 ID를 `~/.figma-cli/session.json`에 캐싱 (SessionManager)
- nodeId 미지정 시 Figma Desktop에서 현재 선택된 노드 사용
- fileKey 파라미터 불필요 (로컬 MCP 서버 특성)
- 기본 타임아웃 60초 (get_design_context 대형 응답 대비)
- get_screenshot: content type이 image → Buffer.from(data, 'base64')
- get_metadata: content가 2개 (XML + guidance)
- isError: true → ToolExecutionError throw
- MCP SDK `callTool` 반환이 union 타입 → `'content' in result` 가드 필요
- 명령어 핸들러는 client 주입 패턴으로 분리 → 단위 테스트 용이
- `registerCommands(program)` 패턴으로 모듈별 명령어 등록
- get_design_context: content 8개 (코드 1 + guidance 7), get_metadata: 2개 (XML + guidance), 나머지: 1개
  - **주의**: MCP 서버는 구분 없이 text 배열로만 반환. "첫 번째=코드, 나머지=guidance" 구분은 관찰 패턴 기반 추정이며, 서버 업데이트 시 변경될 수 있음
- get_design_context에 `artifactType`(4종)과 `taskType`(3종) 옵션 존재

## MCP SDK API 조사 결과

Explore 에이전트로 `node_modules/@modelcontextprotocol/sdk/` 타입 정의를 확인 완료:

### Client 클래스
```typescript
constructor(_clientInfo: Implementation, options?: ClientOptions)
connect(transport: Transport, options?: RequestOptions): Promise<void>
callTool(params: CallToolRequest['params'], resultSchema?, options?: RequestOptions): Promise<CallToolResult>
close(): Promise<void>
```

### StreamableHTTPClientTransport
```typescript
constructor(url: URL, opts?: StreamableHTTPClientTransportOptions)
// opts: { authProvider?, requestInit?: RequestInit, sessionId?: string, reconnectionOptions?, fetch? }
get sessionId(): string | undefined
```

### CallToolResult
```typescript
{ content: ContentBlock[]; isError?: boolean; structuredContent?: Record<string, unknown> }
// TextContent = { type: "text"; text: string }
// ImageContent = { type: "image"; data: string (base64); mimeType: string }
```
